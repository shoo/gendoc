[
	{
		"name": "normal",
		"dubWorkDir": "${project_root}",
		"workDir": "${test_dir}",
		"args": [
			"--config=valid",
			"--root=${test_dir}"
		]
	},
	{
		"name": "default_gendoc",
		"dubWorkDir": "${project_root}",
		"workDir": "${test_dir}",
		"args": [
			"--root=${test_dir}"
		]
	}
]
